---
import Layout from '../layouts/Layout.astro';
import SearchComponent from '../components/Search';
import { getCollection } from 'astro:content';

const tkjPosts = await getCollection('tkj');
const risalahPosts = await getCollection('risalah');
const gardenPosts = await getCollection('garden');

// Prepare items for client-side search component
const searchItems = [
  ...tkjPosts.map((p) => ({
    slug: p.id,
    title: p.data.title,
    description: p.data.description,
    collection: 'tkj' as const,
    pubDate: p.data.pubDate.toISOString(),
    tags: p.data.tags,
    category: p.data.category,
    duration: p.data.duration,
  })),
  ...risalahPosts.map((p) => ({
    slug: p.id,
    title: p.data.title,
    description: p.data.description,
    collection: 'risalah' as const,
    pubDate: p.data.pubDate.toISOString(),
    tags: p.data.tags,
    duration: p.data.duration,
  })),
  ...gardenPosts.map((p) => ({
    slug: p.id,
    title: p.data.title,
    description: p.data.description,
    collection: 'garden' as const,
    pubDate: p.data.pubDate.toISOString(),
    tags: p.data.tags,
    status: p.data.status,
  })),
];
---

<Layout title="Pencarian Catatan — kanghadad.com">
  <div class="container">
    <div style="padding: 40px 0;">
      <h1 style="font-size: 2.2rem; font-weight: 800; letter-spacing: -0.02em; margin-bottom: 8px;">Pencarian Terpadu</h1>
      <p style="color: var(--text-muted); margin-bottom: 40px;">Cari materi TKJ, esai risalah, atau status digital garden berdasarkan kata kunci atau tag.</p>
      
      <!-- Client-side React Search Component with client:load directive -->
      <SearchComponent items={searchItems} client:load />
    </div>
  </div>
</Layout>
